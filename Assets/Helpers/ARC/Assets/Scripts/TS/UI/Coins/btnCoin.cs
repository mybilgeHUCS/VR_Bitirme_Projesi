// Description: btnCoin: Script uses as a tag.
using UnityEngine;

public class btnCoin : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
